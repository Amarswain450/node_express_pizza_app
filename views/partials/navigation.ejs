<nav class="navbar navbar-expand-lg navbar-light bg-light py-4">
    <div class="container">
      <a class="navbar-brand" href="/">Domino's Pizza</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Menu</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/offers">Offers</a>
          </li>
          <% if(user){ %>
            <li class="nav-item">
              <form action="/logout" method="POST" id="logout">
                <a onclick="document.getElementById('logout').submit()" class="nav-link" href="#">Logout</a>
              </form>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/register">Register</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
          <% } %>
          <li class="nav-item">
            <button class="btn btn-orange">
              <span id="cartCounter"><%= session.cart ? session.cart.totalQty : '' %></span><a class="nav-link" href="/cart">Cart</a>
            </button>
          </li>
        </ul>
      </div>
    </div>
  </nav>